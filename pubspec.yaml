name: "khadem_app"
publish_to: none
description: A new Khadem project.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  khadem: ^2.0.0
dev_dependencies:
  test: ^1.29.0
  lints: ^6.0.0
  build_runner: ^2.4.6
